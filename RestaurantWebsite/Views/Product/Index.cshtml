@model IEnumerable<RestaurantWebsite.Models.ProductModel>

<div class="container">
    <h1>Products</h1>
    @foreach (var product in Model)
    {
        @product.Name
        @product.Price
    }
</div>